<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Target modern Android via .NET 8 -->
    <TargetFramework>net8.0-android</TargetFramework>
    <SupportedOSPlatformVersion>21.0</SupportedOSPlatformVersion>
    <OutputType>Exe</OutputType>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    
    <!-- FIX 1: Prevents duplicate assembly metadata property conflicts -->
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    
    <AssemblyName>Sonic3HD</AssemblyName>
    <RootNamespace>Sonic3HD</RootNamespace>
    
    <ApplicationId>com.sonic3hd.androidport</ApplicationId>
    <ApplicationVersion>1</ApplicationVersion>
    <ApplicationDisplayVersion>1.0</ApplicationDisplayVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- Grab absolutely everything inside your decompiled src path -->
    <Compile Include="$(MSBuildThisFileDirectory)../src/**/*.cs" />
    
    <!-- FIX 2: Exclude the compiler module token files -->
    <Compile Remove="$(MSBuildThisFileDirectory)../src/**/&lt;Module&gt;*.cs" />
    
    <!-- FIX 3: Exclude broken Windows Forms dependencies -->
    <Compile Remove="$(MSBuildThisFileDirectory)../src/**/GClass25.cs" />
    
    <!-- FIX 4: Exclude illegal explicit MulticastDelegate classes -->
    <Compile Remove="$(MSBuildThisFileDirectory)../src/**/Delegate*.cs" />

    <!-- Modern MonoGame Engine Backend for Android -->
    <PackageReference Include="MonoGame.Framework.Android" Version="3.8.1.*" />
  </ItemGroup>

  <ItemGroup>
    <!-- Map the assets explicitly from the target directory tree -->
    <AndroidAsset Include="src/Content/**/*" />
    <AndroidAsset Include="$(MSBuildThisFileDirectory)../src/Content/**/*" />
  </ItemGroup>
</Project>
